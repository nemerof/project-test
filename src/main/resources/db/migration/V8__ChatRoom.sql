create table chat_room (
                         id int8 not null,
                         first varchar(255),
                         second varchar(255),
                         primary key (id)
);